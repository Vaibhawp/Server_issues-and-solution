cat /etc/kubernetes/admin.conf ---> (to verify the existence and validity of the kubeconfig file)
sudo kubeadm init phase kubeconfig admin --kubeconfig /etc/kubernetes/admin.conf ---> (to regenerate the admin kubeconfig if necessary)
sudo kubeadm certs renew all ---> (to manually renew Kubernetes certificates)
sudo systemctl restart kubelet ---> (to restart control plane components)
minikube logs --file=logs.txt ---> (to check and save Minikube logs to a file)
minikube delete ---> (to reset Minikube)
minikube start ---> (to start Minikube after reset)
minikube ssh ---> (to access the Minikube VM)
sudo kubeadm certs renew all ---> (to regenerate certificates inside the VM)
exit ---> (to exit the Minikube SSH session)
minikube start --driver=docker ---> (to start Minikube with Docker on Windows/Linux)
minikube start --driver=hyperv --hyperv-virtual-switch="YourSwitchName" ---> (to start Minikube with Hyper-V on Windows)
minikube start --driver=kvm2 ---> (to start Minikube with KVM2 on Linux)
sudo minikube start --driver=none ---> (to run Minikube without a driver on Linux)